// Crear base de datos
use("dbStreamCloud")

//Listar las series que tengan mas de 4 temporadas
db.catalogo.find(
    {temporadas: {$gt: 4}}, //gt = (mayor que)
    {titulo:1, genero:1, vistas:1, _id:0} //proyección
).sort({vistas:-1}) //-1 =  ordenamiento descendente